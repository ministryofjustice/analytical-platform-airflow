---
name: 🦝 Super Linter

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  super-linter:
    name: Super Linter
    permissions:
      contents: read
      packages: read
      pull-requests: write
      statuses: write
    uses: ministryofjustice/data-platform-github-actions/.github/workflows/_super-linter.yml@6df010e45fdb953369f12ab2bc69107d3c487f15 # v4.0.8
    with:
      # VALIDATE_KUBERNETES_KUBECONFORM is set to false as it cannot process Helm charts
      # VALIDATE_PYTHON_* are set to false as we need to revisit the Python from a linting perspective
      super-linter-variables: |
        {
          "VALIDATE_SPELL_CODESPELL": false,
          "VALIDATE_JSCPD": false,
          "VALIDATE_KUBERNETES_KUBECONFORM": false,
          "VALIDATE_PYTHON": false,
          "VALIDATE_PYTHON_BLACK": false,
          "VALIDATE_PYTHON_FLAKE8": false,
          "VALIDATE_PYTHON_ISORT": false,
          "VALIDATE_PYTHON_MYPY": false,
          "VALIDATE_PYTHON_PYINK": false,
          "VALIDATE_PYTHON_PYLINT": false,
          "VALIDATE_PYTHON_RUFF": false,
          "VALIDATE_PYTHON_RUFF_FORMAT": false,
          "VALIDATE_GITHUB_ACTIONS_ZIZMOR": false,
          "VALIDATE_TRIVY": false
        }
